'use client'
// @ts-nocheck
import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Login | ExamCoach',
}

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}


